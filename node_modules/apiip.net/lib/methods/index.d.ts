import { Settings, SettingsOptions } from '../settings';
interface MethodOptions {
    output: string;
    ip: string;
    fields: string;
    language: string;
    callback: string;
}
declare class Methods extends Settings {
    constructor(accessKey: string, options: SettingsOptions);
    getLocation(options?: MethodOptions): Promise<import("axios").AxiosResponse<any, any>>;
}
export default Methods;
