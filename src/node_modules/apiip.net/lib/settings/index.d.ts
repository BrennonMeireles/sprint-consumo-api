interface SettingsOptions {
    ssl: boolean;
}
declare class Settings {
    private accessKey;
    private baseUrl;
    private BASE_URL;
    private BASE_URL_SSL;
    protected constructor(accessKey: string, options?: SettingsOptions);
    private getBaseUrl;
    protected getDefaultApiUrl(): string;
}
export { Settings, SettingsOptions };
